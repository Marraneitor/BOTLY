[build]
buildCommand = "npm install && npm --prefix whatsapp-bot-saas install"

[deploy]
startCommand = "npm --prefix whatsapp-bot-saas start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
